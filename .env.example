# TIMES_SHOP environment variables
# Copy to .env and fill in. NEVER commit .env (see .gitignore).

# Supabase (same project as TIMES_POS — do NOT create a new database)
VITE_SUPABASE_URL=https://zrymhhkqdcttqsdczfcr.supabase.co
VITE_SUPABASE_ANON_KEY=

# Branding
VITE_SHOP_NAME=TIMES STORE

# API mode: false = shop-get-catalog (TikTok SKU via Edge Function). true = local mock (dev only).
VITE_USE_MOCK_API=false

# Google OAuth redirect (must match Supabase Auth config). Leave blank to disable the button.
VITE_GOOGLE_REDIRECT_URL=https://evasi0m.github.io/TIMES_SHOP/
