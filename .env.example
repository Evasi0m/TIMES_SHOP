# TIMES_SHOP environment variables
# Copy to .env and fill in. NEVER commit .env (see .gitignore).

# Shop Supabase — auth, promos, addresses, admin (Hybrid B)
VITE_SUPABASE_URL=https://nfhrzxwunuvasdrujxwh.supabase.co
VITE_SUPABASE_ANON_KEY=

# POS Supabase — TikTok catalog via shop-get-catalog / shop-get-product
VITE_POS_SUPABASE_URL=https://zrymhhkqdcttqsdczfcr.supabase.co
VITE_POS_SUPABASE_ANON_KEY=

# Branding
VITE_SHOP_NAME=TIMES STORE

# API mode: false = Edge Functions. true = local mock (dev only).
VITE_USE_MOCK_API=false

# Google OAuth redirect (must match Supabase Auth). Leave blank to disable.
VITE_GOOGLE_REDIRECT_URL=
