---
description: TIMES_SHOP — scope, security, and backend contract
alwaysApply: true
---

# TIMES_SHOP rules

## Project context
- TIMES_SHOP = customer-facing web store (separate repo from TIMES_POS)
- **Hybrid B:** Shop Supabase (`VITE_SUPABASE_*`) = auth, promos, addresses, admin. POS Supabase (`VITE_POS_*`) = TikTok catalog cache only.
- Product catalog syncs from TikTok Shop API via POS `shop-get-catalog` (not from browser direct TikTok)

## Required reading before any feature work
1. `docs/times-shop-handoff/SHOP_REQUIREMENTS.md`
2. `docs/times-shop-handoff/BACKEND_CONTRACT.md`
3. `docs/times-shop-handoff/SECURITY.md`
4. `docs/times-shop-handoff/UI_GUIDE.md`

## Hard rules (security)
- NEVER query `products`, `sale_orders`, `sale_order_items` directly from the shop client
- NEVER use service_role key in frontend
- NEVER call `tiktok-products-search` or other POS/TikTok Edge Functions from the shop
- NEVER expose cost_price, supplier data, or other shops' orders
- NEVER copy or import from TIMES_POS `main.jsx` or POS-only components
- ALL catalog/checkout APIs go through `shop-*` Edge Functions per BACKEND_CONTRACT
- Re-validate cart server-side before placing orders — never trust client prices

## Auth
- Shop admin allowlist: `true.wifi8888@gmail.com` → `app_role: admin` (trigger on Shop project)
- Other signups → `app_role: customer` in app_metadata (set by backend trigger)
- Staff POS roles stay on POS project — separate from Shop admin
- Use Shop anon key + JWT in browser; NEVER service_role in frontend

## API routing (Hybrid B)
- Catalog / validate cart → POS project (`posSupabase` / `VITE_POS_*`)
- Auth, promos, settings, addresses, place order → Shop project (`supabase` / `VITE_SUPABASE_*`)
- `shop-place-order` on Shop bridges to POS with server secret (not in browser)

## UI
- Thai language primary
- Mobile-first, cream + coral tokens from UI_GUIDE
- Min touch target 44px
- Copy money formatting pattern from POS `money.js` — do not duplicate POS UI shell

## Backend changes
- Shop DB migrations + Shop Edge Functions: `supabase/` in TIMES_SHOP
- POS catalog/order functions: deploy to POS project (`supabase-pos/` reference or TIMES_POS repo)
- Toggle mock via `VITE_USE_MOCK_API=true` for offline dev only

## Order flow
- Order flow: web orders use TikTok stock for catalog; POS still cuts POS stock on confirm
- Payment slips: admin manual review in `/admin` — no paid OCR services
- Auth: disable Supabase Confirm email — checkout without email verification
